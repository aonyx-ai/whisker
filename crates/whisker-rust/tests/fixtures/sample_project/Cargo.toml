[workspace]

[package]
name = "sample_project"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
# `syn` declares `Error` in a private `error` module and re-exports it at the
# crate root, so it is the case that separates comparing definition paths from
# comparing public ones.
syn = "3"
